import  React  from 'react';
import  PropTypes  from 'prop-types';

import {{properCase name }} from '../containers/{{properCase name }}';
import { withTranslation } from 'utils/with-i18next';

export class {{properCase name }}Page extends React.PureComponent {
render() {

return <{{properCase name }} />;
}
}

{{properCase name }}Page.propTypes = {
};

{{properCase name }}Page.getInitialProps = async () => ({
namespacesRequired: ['common', 'banner', 'features'],
});

export default withTranslation('common')({{properCase name }}Page);
